# Figure captions — Phase 7

Six SVGs in `figures/output/`, each regenerated by the matching script in `figures/`
(shared dark styling from `figures/style.py`). Every number below came from a run of
that script on this machine; see each caption for the exact command and hardware.
**Hardware for every GPU figure: AMD Radeon RX 7800 XT, ROCm 7.2, torch
2.9.1+rocm7.2.1, pinned with `HIP_VISIBLE_DEVICES=0`** (device 1 on this machine is
integrated graphics).

Paste the `<figure>` block for each project into the matching `<!-- PANEL MEDIA -->`
slot in `docs/reference/index.html`, replacing the placeholder `<figcaption>` that is
there now.

---

## 1. `iapws95-benchmark.svg`

Regenerate: `HIP_VISIBLE_DEVICES=0 python figures/iapws95_benchmark.py`

```html
<figure class="panel-media">
  <img src="assets/projects/iapws95-benchmark.svg" alt="GPU vs CPU runtime benchmark, log-log scale" />
  <figcaption>
    IAPWS-95 property evaluation (&rho;, h, c<sub>p</sub>, &mu;, k), this library's
    GPU-batched implementation vs. the reference <code>iapws</code> PyPI package
    (pure-Python, one state point at a time). Measured on an AMD Radeon RX 7800 XT
    (ROCm 7.2, torch 2.9.1) against `iapws` on the same machine's CPU, both libraries run
    at every batch size up to 20,000 points: speedup grows from 1.2&times; at 100 points
    to <strong>59.9&times; at 20,000 points</strong> (both sides measured). Beyond that
    the pure-Python reference becomes impractical to run directly (an actual run at
    1,000,000 points would take on the order of 45 minutes); extrapolating its measured
    per-point cost gives an estimated <strong>~67&times; at 1,000,000 points</strong>,
    shown as a dashed line and explicitly labeled "extrapolated," not measured.
    Accuracy cross-checked against `iapws` at 300 random points, 280&ndash;800 K,
    0.5&ndash;100 MPa: max relative error 0.038% (c<sub>p</sub>), &lt;0.01% on the rest.
  </figcaption>
</figure>
```

**Note on the CV/page claim:** the CV states "36x+ speedup" and the repository owner
recalled ~96x at 1e6 points from an earlier (now-unavailable) benchmark run. Neither
number is what this run measured. The largest speedup **actually measured** (both
libraries run, no extrapolation) is **59.9x at N=20,000**; the largest **extrapolated**
figure (CPU cost extrapolated from its own measured per-point cost, GPU side measured
directly) is **~67x at N=1,000,000**. The panel's `panel-stat` badge text
("36×+ speedup") and the bullet claiming "36×+ speedup" in the GPU IAPWS-95 project
description should be updated to reflect the measured 59.9x / extrapolated ~67x, not
left at 36x or bumped to 96x — neither is supported by this run. See the "Report back"
section below.

---

## 2. `iapws95-derivative-validation.svg`

Regenerate: `HIP_VISIBLE_DEVICES=0 python figures/iapws95_derivative_validation.py`

```html
<figure class="panel-media">
  <img src="assets/projects/iapws95-derivative-validation.svg" alt="Autograd vs finite-difference derivative validation" />
  <figcaption>
    &part;&rho;/&part;T|<sub>p</sub> from autograd (implicit function theorem, reattaching
    rho_Tp's detached Newton solve to T via F(&rho;,T)=p(&rho;,T)-p<sub>target</sub>=0)
    against an independent central finite-difference reference, p=25 MPa (supercritical),
    T from 300&ndash;800 K, 200 points, AMD Radeon RX 7800 XT / torch 2.9.1+rocm7.2.1.
    Mean relative error 5.4&times;10<sup>-5</sup>; the error rises to a measured maximum
    of 4.9&times;10<sup>-3</sup> in a narrow band around the pseudo-critical temperature
    (~647 K) where &part;&rho;/&part;T is steep enough that the finite-difference
    reference itself, not the autograd derivative, loses precision.
  </figcaption>
</figure>
```

---

## 3. `liquid-metal-uncertainty.svg`

Regenerate: `python figures/liquid_metal_uncertainty.py` (CPU; no GPU work involved —
liqprops.py's k(T) formulas and `pinthac.uncertainty.perturb` run identically on numpy)

```html
<figure class="panel-media">
  <img src="assets/projects/liquid-metal-uncertainty.svg" alt="Property uncertainty bands for sodium, lead, and LBE" />
  <figcaption>
    Thermal conductivity vs. temperature for sodium, lead, and LBE (Sobolev 2020
    correlations, `pinthac/properties/liqprops.py`), with a 95% Monte Carlo band
    (4,000 trials per temperature, `pinthac.uncertainty.perturb`) from each metal's
    documented model-form uncertainty (`uncert_k`). Sigma is linearly interpolated
    between each metal's stated low/high `uncert_k` bound across its valid temperature
    range — a stated modeling choice (see the script's docstring), not a value given
    directly by the source. At the top of each metal's valid range: Na 50.18 W/m-K
    [95% band 42.3, 58.1], Pb 23.49 W/m-K [16.6, 30.5], LBE 20.55 W/m-K [14.6, 26.9].
  </figcaption>
</figure>
```

---

## 4. `sca-surrogate-validation.svg`

Regenerate: `HIP_VISIBLE_DEVICES=0 python figures/sca_surrogate_validation.py`

```html
<figure class="panel-media">
  <img src="assets/projects/sca-surrogate-validation.svg" alt="DeepONet surrogate vs ground-truth axial temperature profile" />
  <figcaption>
    Rod DeepONet-PINN surrogate (checkpoint sca_rod_deeponet_best.pth, best validation
    loss 1.069e-4) vs. the ground-truth FVM solve (`pinthac.sca.rod.run_SCA_batch`),
    coolant temperature T<sub>i</sub> and peak fuel temperature T<sub>fuel,max</sub>,
    for three held-out operating points (never seen in training). Over 2,000 held-out
    runs: T<sub>i</sub> MAE 0.113 K (0.018% mean relative), T<sub>fuel,max</sub> MAE
    5.03 K (0.356% mean relative). AMD Radeon RX 7800 XT for the surrogate forward pass.
  </figcaption>
</figure>
```

---

## 5. `sca-inference-speed.svg`

Regenerate: `HIP_VISIBLE_DEVICES=0 python figures/sca_inference_speed.py`

Measured on the GPU (AMD RX 7800 XT, ROCm 7.2, `HIP_VISIBLE_DEVICES=0`), held-out
runs only:

    batch   iterative FVM   DeepONet surrogate   speedup
       10       4057 ms            1.88 ms        2158x
      100       4423 ms           15.84 ms         279x
    1,000       5334 ms          136.25 ms          39x
   10,000       5265 ms         1343.38 ms         3.9x
   20,000       5025 ms         2678.96 ms         1.9x

```html
<figure class="panel-media">
  <img src="assets/projects/sca-inference-speed.svg" alt="Surrogate vs iterative solver wall-clock, against batch size" />
  <figcaption>
    Wall-clock cost of a full 100-node axial solve: the DeepONet surrogate against the
    iterative finite-volume solver it was trained on, both on an AMD Radeon RX 7800 XT
    (ROCm 7.2, torch 2.9.1). Plotted against batch size rather than as a single number,
    because the answer depends entirely on it: <strong>2158&times; for a single-rod
    query (10 rods), 39&times; at 1,000, and 1.9&times; at 20,000</strong>. The
    iterative solver is itself vectorized over the batch, so its cost is nearly flat
    from 10 to 20,000 rods (4.1 to 5.0 s) while the surrogate's grows linearly — the
    surrogate wins overwhelmingly for the interactive, one-case-at-a-time queries a
    design study makes, and the advantage narrows toward break-even for the large
    sweeps the solver was already good at.
  </figcaption>
</figure>
```

**Deliberately not captioned with a single headline number.** A lone "Nx faster" would
be true at exactly one batch size and misleading everywhere else.

---

## 6. `pinthac-architecture.svg`

Regenerate: `python figures/pinthac_architecture.py` (static analysis of the repository's
own `import` statements via Python's `ast` module — no GPU, no physics)

```html
<figure class="panel-media">
  <img src="assets/projects/pinthac-architecture.svg" alt="PINTHAC module architecture diagram" />
  <figcaption>
    PINTHAC's actual module import graph, extracted by parsing every `pinthac/**/*.py`
    file's `import` statements (not a hand-drawn idealization). PINTHAC packages in
    accent color, external dependencies in muted grey. Confirms the documented one-way
    layering (properties &larr; correlations &larr; pin &larr; sca &larr; ml,
    CLAUDE.md section 8): no edge in the parsed graph points from an earlier layer to a
    later one.
  </figcaption>
</figure>
```

---

## Summary of claims the measurements do not support

1. **"36×+ speedup" (IAPWS-95 GPU project badge and bullet).** Not supported as
   written. Measured: up to 59.9x (actually run, N=20,000); ~67x extrapolated at
   N=1,000,000. The owner's recalled ~96x is also not what this run reproduced. See
   figure 1's caption note above.
2. **"Wrote finite-difference and physics-informed neural network (PINN) solvers for
   transient single-channel analysis"** (SCA suite project bullet). Per
   `docs/DECISIONS.md` ("Scope: transient is elsewhere"), transient SCA lives in a
   separate repository not covered here; this bullet is not supported by anything in
   this repository and should be reworded or removed, per that decision (not a new
   finding of this phase — flagged here again because it is exactly the kind of
   page claim this phase was asked to check for).
3. **"Implemented multiple literature-published correlations per property"** (liquid
   metal project bullet). Per `docs/DECISIONS.md` ("Scope: liquid metals"), this is not
   supported by the repository (one correlation per property) and was already flagged
   for rewording at Phase 7 by that decision.

---

## Timing methodology, added after the first pass

The GPU side of both benchmark figures originally timed each batch size **once**. That is
not reproducible on this machine: one run recorded 2.50 s at 20,000 points and 1.95 s at
50,000 -- a larger problem finishing faster, which is a property of the driver at that
moment (allocation, clock ramp, another process on the card) rather than of the code. The
same script, unchanged, reported 59.9x, 57.7x and 21.9x at the same batch size across three
runs.

Both scripts now take the **best of five** timed runs, with `torch.cuda.empty_cache()`
between repeats so the sweep cannot accumulate allocations into an out-of-memory at the
largest sizes. The minimum is the right statistic here: interference can only ever make a
run slower, so the fastest of several is both the cleanest estimate of what the code costs
and stable between runs in a way the mean is not.

With that fix the IAPWS-95 figure reproduces 59.9x at N = 20,000, and the extrapolated
curve is smooth at 69x / 68x / 67x rather than jumping discontinuously from the measured
segment. All numbers quoted in this document come from that corrected run.

**Run both figures sequentially, not concurrently** -- two processes sweeping to a million
points will exhaust the card's 16 GB.
